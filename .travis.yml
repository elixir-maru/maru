language: erlang
notifications:
  recipients:
    - self@falood.me
otp_release:
  - 17.0
before_install:
  - git clone --branch "v1.0.0" https://github.com/elixir-lang/elixir
  - cd elixir && make && cd ..
before_script: "export PATH=`pwd`/elixir/bin:$PATH"
script: "MIX_ENV=test mix do local.hex --force, deps.get, test"
